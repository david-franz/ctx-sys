/**
 * ProjectManager - Re-export for alternative naming
 */

export * from './manager';
export { ProjectManager } from './manager';
import { ProjectManager } from './manager';
export default ProjectManager;
