/**
 * Type Categorizer
 *
 * Categorizes entity types into broad categories.
 */

export class TypeCategorizer {
  categorize(type: string): 'code' | 'documentation' | 'conversation' | 'other' {
    throw new Error('Not implemented');
  }
}
