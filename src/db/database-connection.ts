/**
 * DatabaseConnection - Re-export for alternative naming
 */

export * from './connection';
export { DatabaseConnection } from './connection';
import { DatabaseConnection } from './connection';
export default DatabaseConnection;
